<Window x:Class="SpaceAudio.Views.MaterialManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SpaceAudio.ViewModels"
        xmlns:localization="clr-namespace:SpaceAudio.Localization"
        xmlns:c="clr-namespace:YukkuriMovieMaker.Controls;assembly=YukkuriMovieMaker.Controls"
        d:DataContext="{d:DesignInstance Type=vm:MaterialManagerViewModel}"
        mc:Ignorable="d"
        Title="{x:Static localization:Texts.MaterialManagerTitle}"
        Height="500" Width="440" MinHeight="420" MinWidth="400"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
        Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}">
    <Window.Resources>
        <Style TargetType="Button" x:Key="MatBtn">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3" SnapsToDevicePixels="True">
                            <ContentPresenter Focusable="False" HorizontalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush"
                                        Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background"
                                        Value="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{x:Static localization:Texts.MaterialsList}" FontWeight="Bold" Margin="0,0,0,6"/>
        <ListBox Grid.Row="1" ItemsSource="{Binding Materials}" SelectedItem="{Binding SelectedMaterial}"
                 Background="Transparent" BorderThickness="1"
                 HorizontalContentAlignment="Stretch"
                 BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <DockPanel Margin="4,0" Height="28" LastChildFill="True">
                        <TextBlock DockPanel.Dock="Right" Opacity="0.65"
                                   VerticalAlignment="Center" Margin="8,0,0,0"
                                   Text="{Binding Absorption, StringFormat={}{0:P0}}"/>
                        <TextBlock Text="{Binding Name}" VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis"/>
                    </DockPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="{x:Static localization:Texts.MaterialName}"
                       VerticalAlignment="Center" Margin="0,0,8,6"/>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding NewName, UpdateSourceTrigger=PropertyChanged}"
                     Height="30" VerticalContentAlignment="Center"
                     Margin="0,0,0,6"
                     Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
                     Foreground="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"
                     BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
                     CaretBrush="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"/>
            <TextBlock Grid.Row="1" Grid.Column="0" Text="{x:Static localization:Texts.MaterialAbsorption}"
                       VerticalAlignment="Center" Margin="0,0,8,0"/>
            <c:TextBoxSlider Grid.Row="1" Grid.Column="1"
                             Value="{Binding NewAbsorptionDouble, Mode=TwoWay}"
                             Min="0" Max="1" Delta="0.01" StringFormat="P0" Unit=""
                             Margin="0,2,0,6"/>
            <TextBlock Grid.Row="2" Grid.Column="0" Text="色"
                       VerticalAlignment="Center" Margin="0,0,8,0"/>
            <c:ColorPicker Grid.Row="2" Grid.Column="1" Value="{Binding NewColor, Mode=TwoWay}"
                           Height="15" HorizontalAlignment="Stretch" Margin="0,2"/>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="{x:Static localization:Texts.AddMaterial}" Command="{Binding AddCommand}" Style="{StaticResource MatBtn}"/>
            <Button Content="{x:Static localization:Texts.UpdateMaterial}" Command="{Binding UpdateCommand}" Style="{StaticResource MatBtn}"/>
            <Button Content="{x:Static localization:Texts.Delete}" Command="{Binding DeleteCommand}" Style="{StaticResource MatBtn}"/>
        </StackPanel>
    </Grid>
</Window>
