<Window x:Class="SpaceAudio.Views.MaterialManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SpaceAudio.ViewModels"
        xmlns:loc="clr-namespace:SpaceAudio.Localization"
        xmlns:c="clr-namespace:YukkuriMovieMaker.Controls;assembly=YukkuriMovieMaker.Controls"
        d:DataContext="{d:DesignInstance Type=vm:MaterialManagerViewModel}"
        mc:Ignorable="d"
        Title="{x:Static loc:Texts.MaterialManagerTitle}"
        Height="480" Width="700" MinHeight="480" MinWidth="700"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
        Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}">

    <Window.Resources>
        <Style x:Key="MatBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="Margin" Value="3,0"/>
            <Setter Property="MinWidth" Value="60"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3" SnapsToDevicePixels="True">
                            <ContentPresenter Focusable="False"
                                              HorizontalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush"
                                        Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background"
                                        Value="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Opacity" Value="0.8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="1.0"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,8,0,4"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
        </Style>

        <Style x:Key="ParamLabel" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="MinWidth" Value="60"/>
        </Style>

        <Style x:Key="ParamRow" TargetType="Grid">
            <Setter Property="Margin" Value="0,3"/>
        </Style>

        <Style x:Key="BandRow" TargetType="Grid">
            <Setter Property="Margin" Value="0,2"/>
        </Style>

        <Style x:Key="NoteText" TargetType="TextBlock">
            <Setter Property="Opacity" Value="0.65"/>
            <Setter Property="FontStyle" Value="Italic"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,6,0,0"/>
        </Style>

        <Style x:Key="DividerLine" TargetType="Separator">
            <Setter Property="Margin" Value="0,8"/>
            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"/>
        </Style>

        <Style x:Key="SidePanel" TargetType="Border">
            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
        </Style>

        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240" MinWidth="180" MaxWidth="360"/>
            <ColumnDefinition Width="4"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Style="{StaticResource SidePanel}">
            <Grid Margin="12,12,8,12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="{x:Static loc:Texts.MaterialsList}"
                           Style="{StaticResource SectionHeader}"
                           Margin="0,0,0,6"/>

                <ListBox Grid.Row="1"
                         ItemsSource="{Binding Materials}"
                         SelectedItem="{Binding SelectedMaterial}"
                         Background="Transparent"
                         BorderThickness="1"
                         HorizontalContentAlignment="Stretch"
                         BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="6,3"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border x:Name="itemBorder" BorderThickness="1" BorderBrush="Transparent" CornerRadius="2">
                                <DockPanel Height="24" LastChildFill="True" Margin="4,0">
                                    <Rectangle DockPanel.Dock="Left" Width="12" Height="12"
                                               Margin="0,0,6,0" VerticalAlignment="Center"
                                               RadiusX="2" RadiusY="2">
                                        <Rectangle.Fill>
                                            <SolidColorBrush Color="{Binding MaterialColor}"/>
                                        </Rectangle.Fill>
                                    </Rectangle>
                                    <TextBlock DockPanel.Dock="Right" Opacity="0.6"
                                               VerticalAlignment="Center" Margin="6,0,0,0"
                                               FontSize="11"
                                               Text="{Binding Absorption, StringFormat={}{0:P0}}"/>
                                    <TextBlock Text="{Binding LocalizedName}"
                                               VerticalAlignment="Center"
                                               TextTrimming="CharacterEllipsis"/>
                                </DockPanel>
                            </Border>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding IsBuiltIn}" Value="True">
                                    <Setter TargetName="itemBorder" Property="BorderBrush" Value="#1e50a2"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                    <Button Command="{Binding MoveUpCommand}" Style="{StaticResource IconButton}" Margin="0,0,4,0">
                        <Path Data="M 1,5 L 5,1 L 9,5" 
                              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" 
                              StrokeThickness="1.5" StrokeLineJoin="Miter" Stretch="None"/>
                    </Button>
                    <Button Command="{Binding MoveDownCommand}" Style="{StaticResource IconButton}" Margin="0,0,8,0">
                        <Path Data="M 1,1 L 5,5 L 9,1" 
                              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" 
                              StrokeThickness="1.5" StrokeLineJoin="Miter" Stretch="None"/>
                    </Button>
                    <Button Content="{x:Static loc:Texts.AddMaterial}"
                            Command="{Binding AddCommand}"
                            Style="{StaticResource MatBtn}"/>
                </StackPanel>
            </Grid>
        </Border>

        <GridSplitter Grid.Column="1"
                      Width="4"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      Background="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"/>

        <Grid Grid.Column="2" Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <StackPanel>

                    <Grid Style="{StaticResource ParamRow}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Text="{x:Static loc:Texts.MaterialName}"
                                   Style="{StaticResource ParamLabel}"/>
                        <TextBox Grid.Column="1"
                                 Text="{Binding NewName, UpdateSourceTrigger=PropertyChanged}"
                                 Height="28" VerticalContentAlignment="Center"
                                 Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
                                 Foreground="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"
                                 BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
                                 CaretBrush="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"/>
                    </Grid>

                    <Separator Style="{StaticResource DividerLine}"/>

                    <TextBlock Text="{x:Static loc:Texts.MaterialBandAbsorption}"
                               Style="{StaticResource SectionHeader}"/>

                    <Grid Style="{StaticResource BandRow}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="52"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Static loc:Texts.MaterialBand125}" Style="{StaticResource ParamLabel}"/>
                        <c:TextBoxSlider Grid.Column="1" Value="{Binding Band125Double, Mode=TwoWay}"
                                         Min="0" Max="1" Delta="0.01" StringFormat="P0" Unit=""/>
                    </Grid>

                    <Grid Style="{StaticResource BandRow}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="52"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Static loc:Texts.MaterialBand250}" Style="{StaticResource ParamLabel}"/>
                        <c:TextBoxSlider Grid.Column="1" Value="{Binding Band250Double, Mode=TwoWay}"
                                         Min="0" Max="1" Delta="0.01" StringFormat="P0" Unit=""/>
                    </Grid>

                    <Grid Style="{StaticResource BandRow}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="52"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Static loc:Texts.MaterialBand500}" Style="{StaticResource ParamLabel}"/>
                        <c:TextBoxSlider Grid.Column="1" Value="{Binding Band500Double, Mode=TwoWay}"
                                         Min="0" Max="1" Delta="0.01" StringFormat="P0" Unit=""/>
                    </Grid>

                    <Grid Style="{StaticResource BandRow}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="52"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Static loc:Texts.MaterialBand1k}" Style="{StaticResource ParamLabel}"/>
                        <c:TextBoxSlider Grid.Column="1" Value="{Binding Band1kDouble, Mode=TwoWay}"
                                         Min="0" Max="1" Delta="0.01" StringFormat="P0" Unit=""/>
                    </Grid>

                    <Grid Style="{StaticResource BandRow}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="52"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Static loc:Texts.MaterialBand2k}" Style="{StaticResource ParamLabel}"/>
                        <c:TextBoxSlider Grid.Column="1" Value="{Binding Band2kDouble, Mode=TwoWay}"
                                         Min="0" Max="1" Delta="0.01" StringFormat="P0" Unit=""/>
                    </Grid>

                    <Grid Style="{StaticResource BandRow}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="52"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Static loc:Texts.MaterialBand4k}" Style="{StaticResource ParamLabel}"/>
                        <c:TextBoxSlider Grid.Column="1" Value="{Binding Band4kDouble, Mode=TwoWay}"
                                         Min="0" Max="1" Delta="0.01" StringFormat="P0" Unit=""/>
                    </Grid>

                    <Grid Style="{StaticResource ParamRow}" Margin="0,8,0,3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="52"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Static loc:Texts.MaterialAbsorption}" Style="{StaticResource ParamLabel}"
                                   Opacity="0.7" FontSize="11"/>
                        <TextBlock Grid.Column="1"
                                   Text="{Binding NewAbsorption, StringFormat={}{0:P1}}"
                                   VerticalAlignment="Center" Opacity="0.7" FontSize="11"/>
                    </Grid>

                    <Separator Style="{StaticResource DividerLine}"/>

                    <Grid Style="{StaticResource ParamRow}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="52"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Static loc:Texts.MaterialColor}" Style="{StaticResource ParamLabel}"/>
                        <c:ColorPicker Grid.Column="1" Value="{Binding NewColor, Mode=TwoWay}"
                                       Height="18" HorizontalAlignment="Stretch"/>
                    </Grid>

                </StackPanel>
            </ScrollViewer>

            <StackPanel Grid.Row="1" Orientation="Horizontal"
                        HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="{x:Static loc:Texts.DuplicateMaterial}"
                        Command="{Binding DuplicateCommand}"
                        Style="{StaticResource MatBtn}"/>
                <Button Content="{x:Static loc:Texts.UpdateMaterial}"
                        Command="{Binding UpdateCommand}"
                        Style="{StaticResource MatBtn}"/>
                <Button Content="{x:Static loc:Texts.Delete}"
                        Command="{Binding DeleteCommand}"
                        Style="{StaticResource MatBtn}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
